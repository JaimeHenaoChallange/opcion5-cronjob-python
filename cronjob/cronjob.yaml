apiVersion: batch/v1
kind: CronJob
metadata:
  name: deploy-checker
  namespace: argocd
spec:
  schedule: "*/2 * * * *"  # Ejecuta cada 2 minutos
  concurrencyPolicy: Forbid  # No permite que se ejecute un nuevo Job si ya hay uno en ejecución
  successfulJobsHistoryLimit: 1  # Retiene solo 1 Job exitoso
  failedJobsHistoryLimit: 1  # Retiene solo 1 Job fallido
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: deploy-checker
              image: jaimehenao8126/opcion5-deploy-script:21b891b8cd115a11d84e48fee941ab430c63b398
              envFrom:
                - secretRef:
                    name: slack-webhook-secret
              env:
                - name: ARGOCD_SERVER
                  value: "$(MINIKUBE_IP):8080"  # Use Minikube Node IP
                - name: ARGOCD_USERNAME
                  value: "admin"
                - name: ARGOCD_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: argocd-secret
                      key: admin.password
          restartPolicy: OnFailure
          initContainers:
            - name: get-minikube-ip
              image: jaimehenao8126/opcion5-deploy-script:21b891b8cd115a11d84e48fee941ab430c63b398
              command: ["sh", "-c"]
              args:
                - |
                  export MINIKUBE_IP=$(minikube ip) && echo $MINIKUBE_IP > /etc/minikube-ip/env
              volumeMounts:
                - name: minikube-ip
                  mountPath: /etc/minikube-ip
          volumes:
            - name: minikube-ip
              emptyDir: {}
